# Raspberry Pi Pico

The firmware targets the original Raspberry Pi Pico / RP2040 in USB device
mode. Connect the Pico directly to an ASIAIR USB port with a data-capable USB
cable after flashing.

No external motor, wheel, or position sensor is connected to the Pico. The
Pico only emulates USB HID behaviour.
